// Default slot for the parallel @modal route — renders nothing when no
// intercepting route is matched. Next.js requires this for parallel routes.
export default function Default() {
  return null;
}
